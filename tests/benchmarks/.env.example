# API Keys for benchmark tests
# Copy this file to .env and add your actual keys
#
# Usage:
#   cp tests/benchmarks/.env.example tests/benchmarks/.env
#   # Edit tests/benchmarks/.env and add your actual API keys
#
# Security:
#   - NEVER commit the actual .env file to git
#   - .env is already in .gitignore
#   - Only commit .env.example as a template

# Zhipu AI API Key (for embedding models)
# Get your key at: https://open.bigmodel.cn/
ZHIPUAI_API_KEY=your-zhipuai-api-key-here

# OpenAI API Key (optional, for alternative embedding models)
# Get your key at: https://platform.openai.com/
OPENAI_API_KEY=your-openai-api-key-here
